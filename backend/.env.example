# Backend Environment Variables for Render

# CORS - Comma-separated list of allowed frontend URLs
ALLOWED_ORIGINS=https://your-frontend.vercel.app,http://localhost:5173,http://localhost:4173

# Port (Render sets this automatically, but can override for local)
# PORT=8000
